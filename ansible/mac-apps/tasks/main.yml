---
- name: Install Mac apps from Homebrew Cask
  community.general.homebrew_cask:
    name: "{{ item }}"
    state: present
  with_items:
    - gimp
    - podman-desktop
    - slack
    - visual-studio-code
