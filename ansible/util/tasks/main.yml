---
- name: Install general utilities
  become: "{{ become_option }}"
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - colordiff
    - git
    - htop
    - nmap
    - ripgrep
    - telnet
    - tldr
    - tree
    - watch

- name: Install Ubuntu utilities
  when: distribution == 'Ubuntu'
  become: yes
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - python3-apt
    - software-properties-common

# REPLACE WITH A TEMPLATE
# - name: Create link for ~/.editorconfig
#   file:
#     src: "{{ home_dir }}/dotfiles/.editorconfig"
#     dest: "{{ home_dir }}/.editorconfig"
#     state: link
