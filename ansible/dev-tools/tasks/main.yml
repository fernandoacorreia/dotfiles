---
- name: Install development tools and languages
  community.general.homebrew:
    name: "{{ item }}"
    state: present
  with_items:
    - nvm
    - pyenv
    - pyenv-virtualenv
    - rbenv
    - ruby-build
    - uv
    - yarn
